{
  "name": "plume-epique",
  "version": "1.0.0",
  "description": "Plume Épique — suite de tests automatisés (migration vers Vitest)",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "jsdom": "^30.0.0",
    "vitest": "^4.1.10"
  }
}
